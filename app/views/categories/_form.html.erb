<%= form_with(model: @category, local: true) do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>

    <%= f.submit yield(:button_text), class: "btn btn-primary mt-1" %>
  <% end %>
